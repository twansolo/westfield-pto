// Sanity client and utilities
export { client } from './client'
export { urlFor } from './image'
export { projectId, dataset, apiVersion } from './config'

// GROQ queries
export * from './queries'

// TypeScript types
export type { SiteSettings, Event, Announcement, BoardMember, MeetingMinutes, CommitteeChair, Fundraiser } from './types'

